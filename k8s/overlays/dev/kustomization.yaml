apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: locatic

resources:
  - ../../base

images:
  # À mettre à jour une fois l'image publiée par la CI (PR #5) : sha-xxxxxxx ou latest.
  - name: ghcr.io/2021413/locatic
    newTag: latest
