apiVersion: v1
kind: Secret
metadata:
  name: locatic-secrets
  labels:
    app.kubernetes.io/name: locatic-app
    app.kubernetes.io/part-of: locatic
type: Opaque
# Template versionné uniquement : aucune valeur réelle ici.
# En local, remplacer via `kubectl create secret` / un overlay non versionné avant apply.
stringData:
  EXAMPLE_KEY: "changeme"
