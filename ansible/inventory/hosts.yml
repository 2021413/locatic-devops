---
# Inventaire Locatic : tout se joue sur le poste local (minikube + outils CLI).
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
